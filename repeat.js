/* 
    Write a function called repeatStr which repeats a given string exactly n times.
    EG: repeatStr(5, "Hello") // "HelloHelloHelloHelloHello"
*/

const repeatStr = (interval, str) => str.repeat(interval)
console.log(repeatStr(5, 'Hi Wes '))